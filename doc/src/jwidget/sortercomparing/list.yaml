symbols:
  default:
    defaultName: ListSorterComparing
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/sortercomparing/AbstractSorterComparing
    description: |
      <p>%AbstractSorterComparing implementation for %List. See %sortersComparing for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyList<%T>, config: %AbstractSorterComparing.%FullConfig<%T>)
      params:
        source: Source list.
        config: Sorter configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyList<%T>'
        description: Source list.

  sortListComparing:
    signature: |
      <T>(source: %ReadonlyList<T>, config?: %AbstractSorterComparing.%Config<T>): %DestroyableReadonlyList<T>
    params:
      source: Source list.
      config: Sorter configuration.
    returns: Sorted list.
    description: 'Sorts a list (comparing) and starts synchronization. See %sortersComparing for details.'

references:
  Config:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.Config

  FullConfig:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.FullConfig
