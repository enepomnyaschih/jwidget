symbols:
  default:
    typevars:
      T: Item type.
    extends:
      - file: jwidget/IList
        typevars: [T]
      - file: jwidget/Class
    description: |
      <p>Ordered collection of items. Each item of the list has an index. Index of first item is 0,
      index of each next one is higher by 1.</p>
    constructor:
      signature: |
        (silent?: boolean)
        new List<T>(getKey: (item: %T) => any, silent?: boolean)
        new List<T>(items: %T[], flags?: %CollectionFlags)
        new List<T>(items: %T[], getKey: (item: %T) => any, flags?: %CollectionFlags)
      params:
        silent: Create a silent collection which means that it never dispatches modification messages.
        getKey: Function that identifies an item in this collection for optimization of some algorithms.
        items: Initial list contents.
        flags: Collection configuration flags.
