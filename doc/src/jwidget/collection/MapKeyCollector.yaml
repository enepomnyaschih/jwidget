symbols:
  default:
    typevars:
      K: Key type.
    extends:
      - file: jwidget/collection/AbstractValueCollector
    description: |
      <p>Key collector for %ReadonlyBindableMap. See %AbstractValueCollector for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyBindableMap<%K, unknown>, config?: %AbstractValueCollector.%Config<%K>)
      params:
        source: Source map.
        config: Collector configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyBindableMap<%K, unknown>'
        description: Source map.

  startCollectingMapKeys:
    signature: |
      <K>(source: %ReadonlyBindableMap<K, unknown>): %DestroyableReadonlyBindableSet<K>
    params:
      source: Source map.
    returns: Target set.
    description: |
      <p>Creates a set containing all map keys and starts synchronization. See %AbstractValueCollector for details.</p>

references:
  Config:
    file: jwidget/collection/AbstractValueCollector
    symbol: AbstractValueCollector.Config
