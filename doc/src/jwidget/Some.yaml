symbols:
  default:
    kind: interface
    typevars:
      T: Value type.
    description: |
      <p>Value container. Interface which has only one readonly field: %value.</p>

    properties:
      value:
        modifiers: readonly
        type: '%T'
        description: |
          <p>The contained value.</p>
