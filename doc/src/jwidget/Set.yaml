symbols:
  default:
    typevars:
      T: Item type.
    extends:
      - file: jwidget/ISet
        typevars: [T]
      - file: jwidget/Class
    description: |
      <p>Unordered collection optimized for items adding, removal and search.</p>
    constructor:
      signature: |
        (silent?: boolean)
        new Set<T>(getKey: (item: %T) => any, silent?: boolean)
        new Set<T>(items: %T[], silent?: boolean)
        new Set<T>(items: %T[], getKey: (item: %T) => any, silent?: boolean)
      params:
        silent: Create a silent collection which means that it never triggers modification events.
        getKey: Function that identifies an item in this collection for optimization of some algorithms.
        items: Initial list contents.
