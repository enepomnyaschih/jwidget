expandImports: true

symbols:
  createConverterToList:
    signature: |
      <T>(source: %ReadonlyCollection<T>, config?: %AbstractConverterToList.%Config<T>): %AbstractConverterToList<T>
    params:
      source: Source collection.
      config: Converter configuration.
    returns: Collection converter to list.
    description: |
      <p>Creates a converter to list matching the source collection type. See %convertersToList for details.</p>
      <p>If you know exact type of the source collection, please use %ListConverterToList, %MapConverterToList and %SetConverterToList
      constructors instead, because they are more efficient.</p>

  collectionToList:
    signature: |
      <T>(source: %ReadonlyCollection<T>): %DestroyableReadonlySet<T>
    params:
      source: Source collection.
    returns: Target list.
    description: |
      <p>Converts a collection to list and starts synchronization. See %convertersToList for details.</p>
      <p>If you know exact type of the source collection, please use %listToList, %mapToList and %setToList
      functions instead, because they are more efficient.</p>

references:
  Config:
    file: jwidget/convertertolist/AbstractConverterToList
    symbol: AbstractConverterToList.Config
