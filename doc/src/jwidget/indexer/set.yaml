symbols:
  default:
    defaultName: SetIndexer
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/indexer/AbstractIndexer
    description: |
      <p>%AbstractIndexer implementation for %Set. See %indexers for details.</p>
    constructor:
      signature: |
        (source: %ReadonlySet<%T>, getKey: (item: %T) => any, config?: %AbstractIndexer.%Config<%T>)
      params:
        source: Source set.
        getKey: Indexer function.
        config: Indexer configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlySet<%T>'
        description: Source set.

  indexSet:
    signature: |
      <T>(source: %ReadonlySet<T>, getKey: (item: T) => any, scope?: any): %DestroyableReadonlyMap<T>
    params:
      source: Source set.
      getKey: Indexer function.
      scope: Call scope of %getKey callback.
    returns: Collection index map.
    description: 'Indexes set and starts synchronization. See %indexers for details.'

references:
  Config:
    file: jwidget/indexer/AbstractIndexer
    symbol: AbstractIndexer.Config
