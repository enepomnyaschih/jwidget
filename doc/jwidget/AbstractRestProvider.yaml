symbols:
  default:
    kind: abstract class
    typevars:
      C: Request context. In a subclass, you can specify any kind of context for your requests.
    description: TBD
    constructor:
      signature: |
        (config?: %Config)
      params:
        config: Configuration.
      description: Creates class instance.
    methods:
      getHeaders:
        modifiers: abstract
        signature: |
          (context: %C): %Dictionary<string>
        params:
          context: Request context.
        returns: Request headers.

references:
  Config:
    symbol: AbstractRestProvider.Config
