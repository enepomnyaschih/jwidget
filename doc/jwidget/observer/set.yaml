symbols:
  default:
    defaultName: SetObserver
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/observer/AbstractObserver
    description: |
      <p>%AbstractObserver implementation for %Set. See %observers for details.</p>
    constructor:
      signature: |
        (source: %ReadonlySet<%T>, config: %AbstractObserver.%Config<%T>)
      params:
        source: Source set.
        config: Observer configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlySet<%T>'
        description: Source set.

references:
  Config:
    file: jwidget/observer/AbstractObserver
    symbol: AbstractObserver.Config
