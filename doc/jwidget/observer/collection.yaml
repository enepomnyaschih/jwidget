expandImports: true

symbols:
  createObserver:
    signature: |
      <T>(source: %ReadonlyCollection<T>, config?: %AbstractObserver.%Config<T>): %AbstractObserver<T>
    params:
      source: Source collection.
      config: Observer configuration.
    returns: Collection observer.
    description: |
      <p>Creates an observer matching the source collection type. See %observers for details.</p>
      <p>If you know exact type of the source collection, please use %ListObserver, %MapObserver and %SetObserver
      constructors instead, because they are more efficient.</p>

references:
  Config:
    file: jwidget/convertertolist/AbstractObserver
    symbol: AbstractObserver.Config
