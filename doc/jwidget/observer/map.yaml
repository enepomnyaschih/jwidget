symbols:
  default:
    defaultName: MapObserver
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/observer/AbstractObserver
    description: |
      <p>%AbstractObserver implementation for %Map. See %observers for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyMap<%T>, config: %AbstractObserver.%Config<%T>)
      params:
        source: Source map.
        config: Observer configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyMap<%T>'
        description: Source map.

references:
  Config:
    file: jwidget/observer/AbstractObserver
    symbol: AbstractObserver.Config
