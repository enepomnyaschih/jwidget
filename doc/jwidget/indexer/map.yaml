symbols:
  default:
    defaultName: MapIndexer
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/indexer/AbstractIndexer
    description: |
      <p>%AbstractIndexer implementation for %Map. See %indexers for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyMap<%T>, getKey: (item: %T) => any, config?: %AbstractIndexer.%Config<%T>)
      params:
        source: Source collection.
        getKey: Indexer function.
        config: Indexer configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyMap<%T>'
        description: Source collection.

  indexMap:
    signature: |
      <T>(source: %ReadonlyMap<T>, getKey: (item: T) => any, scope?: any): %DestroyableReadonlyMap<T>
    params:
      source: Source collection.
      getKey: Indexer function.
      scope: Call scope of %getKey callback.
    returns: Collection index map.
    description: 'Indexes map and starts synchronization. See %indexers for details.'

references:
  Config:
    file: jwidget/indexer/AbstractIndexer
    symbol: AbstractIndexer.Config
