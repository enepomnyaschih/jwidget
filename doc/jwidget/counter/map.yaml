symbols:
  default:
    defaultName: MapCounter
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/counter/AbstractCounter
    description: |
      <p>%AbstractCounter implementation for %Map. See %counters for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyMap<%T>, test: (item: %T) => any, config?: %AbstractCounter.%Config)
      params:
        source: Source collection.
        test: Filtering criteria.
        config: Counter configuration.
    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyMap<%T>'
        description: Source collection.

  countMap:
    signature: |
      <T>(source: %ReadonlyMap<T>, test: (item: T) => any, scope?: any): %DestroyableBindable<number>
    params:
      source: Source collection.
      test: Filtering criteria.
      scope: Call scope of %test function.
    returns: Target property.
    description: 'Counts matching items in a map and starts synchronization. See %counters for details.'
