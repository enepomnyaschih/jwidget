symbols:
  default:
    defaultName: SetConverterToList
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/convertertolist/AbstractConverterToList
    description: |
      <p>%AbstractConverterToList implementation for %Set. See %convertersToList for details.</p>
    constructor:
      signature: |
        (source: %ReadonlySet<%T>, config: %AbstractConverterToList.%Config<%T>)
      params:
        source: Source set.
        config: Converter configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlySet<%T>'
        description: Source set.

  setToList:
    signature: |
      <T>(source: %ReadonlySet<T>): %DestroyableReadonlyList<T>
    params:
      source: Source set.
    returns: Target list.
    description: 'Converts a set to a list and starts synchronization. See %convertersToList for details.'

references:
  Config:
    file: jwidget/convertertolist/AbstractConverterToList
    symbol: AbstractConverterToList.Config
