symbols:
  default:
    defaultName: MapConverterToList
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/convertertolist/AbstractConverterToList
    description: |
      <p>%AbstractConverterToList implementation for %Map. See %convertersToList for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyMap<%T>, config?: %AbstractConverterToList.%Config<%T>)
      params:
        source: Source map.
        config: Converter configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyMap<%T>'
        description: Source map.

  mapToList:
    signature: |
      <T>(source: %ReadonlyMap<T>): %DestroyableReadonlyList<T>
    params:
      source: Source map.
    returns: Target list.
    description: 'Converts a map to a list and starts synchronization. See %convertersToList for details.'

references:
  Config:
    file: jwidget/convertertolist/AbstractConverterToList
    symbol: AbstractConverterToList.Config
