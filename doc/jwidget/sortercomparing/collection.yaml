expandImports: true

symbols:
  createSorterComparing:
    signature: |
      <T>(source: %ReadonlyCollection<T>, config?: %AbstractSorterComparing.%FullConfig<T>): %AbstractSorterComparing<T>
    params:
      source: Source collection.
      config: Sorter configuration.
    returns: Collection sorter (comparing).
    description: |
      <p>Creates a sorter (comparing) matching the source collection type. See %sortersComparing for details.</p>
      <p>If you know exact type of the source collection, please use %ListSorterComparing, %MapSorterComparing and %SetSorterComparing
      constructors instead, because they are more efficient.</p>

  sortCollectionComparing:
    signature: |
      <T>(source: %ReadonlyCollection<T>, config?: %AbstractSorterComparing.%Config<T>): %DestroyableReadonlySet<T>
    params:
      source: Source collection.
      config: Sorter configuration.
    returns: Sorted list.
    description: |
      <p>Sorts a collection (comparing) and starts synchronization. See %sortersComparing for details.</p>
      <p>If you know exact type of the source collection, please use %sortListComparing, %sortMapComparing and %sortSetComparing
      functions instead, because they are more efficient.</p>

references:
  Config:
    file: jwidget/convertertolist/AbstractSorterComparing
    symbol: AbstractSorterComparing.Config

  FullConfig:
    file: jwidget/convertertolist/AbstractSorterComparing
    symbol: AbstractSorterComparing.FullConfig
