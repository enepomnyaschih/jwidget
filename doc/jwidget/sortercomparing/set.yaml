symbols:
  default:
    defaultName: SetSorterComparing
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/sortercomparing/AbstractSorterComparing
    description: |
      <p>%AbstractSorterComparing implementation for %Set. See %sortersComparing for details.</p>
    constructor:
      signature: |
        (source: %ReadonlySet<%T>, config: %AbstractSorterComparing.%FullConfig<%T>)
      params:
        source: Source set.
        config: Sorter configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlySet<%T>'
        description: Source set.

  sortListComparing:
    signature: |
      <T>(source: %ReadonlySet<T>, config?: %AbstractSorterComparing.%Config<T>): %DestroyableReadonlyList<T>
    params:
      source: Source set.
      config: Sorter configuration.
    returns: Sorted list.
    description: 'Sorts a set (comparing) and starts synchronization. See %sortersComparing for details.'

references:
  Config:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.Config

  FullConfig:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.FullConfig
