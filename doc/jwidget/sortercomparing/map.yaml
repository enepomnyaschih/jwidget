symbols:
  default:
    defaultName: MapSorterComparing
    typevars:
      T: Collection item type.
    extends:
      - file: jwidget/sortercomparing/AbstractSorterComparing
    description: |
      <p>%AbstractSorterComparing implementation for %Map. See %sortersComparing for details.</p>
    constructor:
      signature: |
        (source: %ReadonlyMap<%T>, config: %AbstractSorterComparing.%FullConfig<%T>)
      params:
        source: Source map.
        config: Sorter configuration.

    properties:
      source:
        modifiers: readonly
        type: '%ReadonlyMap<%T>'
        description: Source map.

  sortListComparing:
    signature: |
      <T>(source: %ReadonlyMap<T>, config?: %AbstractSorterComparing.%Config<T>): %DestroyableReadonlyList<T>
    params:
      source: Source map.
      config: Sorter configuration.
    returns: Sorted list.
    description: 'Sorts a map (comparing) and starts synchronization. See %sortersComparing for details.'

references:
  Config:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.Config

  FullConfig:
    file: jwidget/sortercomparing/AbstractSorterComparing
    symbol: AbstractSorterComparing.FullConfig
