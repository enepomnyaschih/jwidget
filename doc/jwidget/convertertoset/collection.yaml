expandImports: true

symbols:
  createConverterToSet:
    signature: |
      <T>(source: %ReadonlyCollection<T>, config?: %AbstractConverterToSet.%Config<T>): %AbstractConverterToSet<T>
    params:
      source: Source collection.
      config: Converter configuration.
    returns: Collection converter to set.
    description: |
      <p>Creates a converter to set matching the source collection type. See %convertersToSet for details.</p>
      <p>If you know exact type of the source collection, please use %ListConverterToSet, %MapConverterToSet and %SetConverterToSet
      constructors instead, because they are more efficient.</p>

  collectionToSet:
    signature: |
      <T>(source: %ReadonlyCollection<T>): %DestroyableReadonlySet<T>
    params:
      source: Source collection.
    returns: Target set.
    description: |
      <p>Converts a collection to set and starts synchronization. See %convertersToSet for details.</p>
      <p>If you know exact type of the source collection, please use %listToSet, %mapToSet and %setToSet
      functions instead, because they are more efficient.</p>

references:
  Config:
    file: jwidget/convertertoset/AbstractConverterToSet
    symbol: AbstractConverterToSet.Config
